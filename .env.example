# Cloudinary (required) — hosts all photos and videos for the site.
# Find this on your Cloudinary dashboard → Programmable Media → Product Environment.
NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME=your-cloud-name

# Vercel (automatically set in Vercel deployments)
# VERCEL_URL=
